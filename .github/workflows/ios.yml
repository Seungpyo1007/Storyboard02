name: Run Test

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  test_ios:
    # macos-latest 러너를 사용하며, 이 러너는 기본적으로 최신 Xcode를 사용합니다.
    runs-on: macos-latest

    steps:
    # 1. Checkout the repository code (using v4, the latest version)
    - uses: actions/checkout@v4
      
    # 2. 수동으로 Xcode 버전을 선택하는 단계를 제거합니다.
    #    이제 러너에 기본 설정된 최신 Xcode 버전(예: 15.x의 최신 버전)을 사용합니다.
    
    # 3. Optional: Verify the selected version (good for debugging)
    #    어떤 Xcode 버전이 사용되는지 확인합니다.
    - name: Show active Xcode version
      run: xcodebuild -version

    # 4. Run the Clean and Test command
    - name: Start xcode build and run tests 🛠
      run: |
        xcodebuild clean test \
          # 프로젝트 이름과 스킴 이름을 'Storyboard02'로 사용합니다.
          -project Storyboard02/Storyboard02.xcodeproj \
          -scheme Storyboard02 \
          -destination 'platform=iOS Simulator,name=iPhone 15 Pro,OS=latest'
